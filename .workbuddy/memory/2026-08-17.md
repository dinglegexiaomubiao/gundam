# 2026-08-17

## gundam 项目上传至项目资产（tdrive）— 已完成
- 将 gundam 项目（22 个文件：根目录 4 + src 11 + scripts 5 + web 3）上传到项目资产 tdrive 的 gundam/ 目录
- 目录 ID：根 JhVfqBlgcDHE / src JemWODZvOGjA / scripts JruveOvEsDVQ / web JEWfuibsOKel
- data/（1827 JSON + 189MB gundam.db）、.pyc、.env、.git 未上传
- 踩坑：COS 临时上传密钥频繁报 InvalidAccessKeyId（含刚申请即失效的情况），可靠做法是**单文件串行**：file_upload → 立即 curl -T 上传 → file_upload_complete；并行批量申请密钥大概率失效。另有 file_upload 限流 5 秒/次。

## 项目规划整理为定期维护型资料库 — 已完成
- 新增 `src/maintain.py`（快照备份滚动 3 份 / 新旧库 diff 报告 / run_update 编排+回滚）
- `pipeline.py` 新增 `update`（日常增量）与 `update --full`（每月全量刷数值）、`backup` 命令；fetch 增加 `--refresh`
- status.py 去硬编码（改读 manifest counts）；清理 gundam.db.tmp 残留
- 新增 MAINTENANCE.md 维护手册；README 更新命令表；.gitignore 补 data/backup/
- 维护约定（用户已确认）：**手动跑不自动定时**；平时增量、每月一次 --full；本地快照保 3 份 + 每月传一份 gundam.db 到 tdrive 项目资产 gundam/backup/
- 冒烟验证：update --full --limit 2 全链路通，真实发现 13 天内新增 15 驾驶员/2 支援/1 剧情事件/6 Boss
- 改动尚未 git commit

## 手动增量更新 — 已完成（15:34-15:55，约 21 分钟，0 失败）
- 新增 16 台机体（骑士高达系列为主）、15 名驾驶员（全量列表直接覆盖）、2 支援、15 关卡、+267 敌人
- 库已重建：unit 1226 / stage 609；快照 data/backup/gundam_20260817_153447.db
- 提示用户：Web 概览页手动云端同步（如需）
