# 产品规划：高达 G 世纪永恒 资料库

> 制定日期：2026-09-10
> 定位：**单人本地决策工具**（单机 127.0.0.1，用户即开发者）
> 本文是产品视角的路线图，不是代码重构清单。
>
> **执行状态**（2026-09-10 16:15 更新）：
> - 🔴 **P0.1 数据更新阻塞**：数据源 soshage API 整体 404，见 [§7](#7-阻塞数据源失效)
> - ✅ P0.3 备份重建完成（421 MB → 24 MB）
> - ✅ P0.4 组队验证完成，**并修复了一个让组队功能完全不可用的 bug**
> - ✅ P0.5 残留清理完成
> - ⏸ P0.2 自动化暂缓（数据源恢复前必然失败）

---

## 1. 产品是什么

一句话：**把散落在 soshage 站点的游戏数据，变成"我该练哪台机体、配哪个驾驶员"的决策依据。**

用户不是在看代码，是在玩游戏时做选择。所有投入必须能追溯到这个场景，否则就不做。

### 当前功能面（10 页 / 47 个 API）

| 页面 | 用户在这里干什么 | 状态 |
|---|---|---|
| 概览 | 看数据是否齐全、导入导出、云端同步 | 可用 |
| 机体 / 驾驶员 / 支援角色 | 查属性、筛选、对比 | 可用 |
| 关卡 | 查敌人配置与掉落 | 可用 |
| 搜索 | 跨实体检索 | 可用 |
| 伤害 | 按 formulas.docx 算伤害 | 可用 |
| 配对 | 机体 ↔ 驾驶员推荐（100 分模型） | 可用 |
| 组队 | 组一队算总分 | **未验证** |
| 原作映射 | 机体 ↔ 驾驶员的原作对应关系 | 可用 |

---

## 2. 价值链诊断：哪一环在漏水

```
抓取 → 入库 → 云端备份 → 浏览/搜索 → 配对·伤害·组队 → 游戏内决策
 ❶       ❷        ❸          ❹              ❺              ❻
```

| 环节 | 现状 | 判定 |
|---|---|---|
| ❶ 抓取 | `fetched_at = 2026-09-02`，**已 8 天未更新**；无任何自动触发 | ❌ **漏水** |
| ❷ 入库 | 27 张表，unit 1237 / character 593 / supporter 87 / stage 625 / unit_pilot 1221，行数合理 | ✅ 健康 |
| ❸ 云端 | Neon 23 表同步正常；但**需手动点按钮**，且最新备份是瘦身前的 207 MB，当前 24.6 MB 库无备份 | ⚠️ 半漏水 |
| ❹ 浏览 | 筛选维度完整（系列/类型/标签/获取途径/武器特效/技能） | ✅ 健康 |
| ❺ 计算 | 评分模型 + 伤害公式 + 星级倍率，**零测试** | ⚠️ 无护栏 |
| ❻ 组队 | `team` / `team_config` **本地与云端均 0 行** | ❓ **未验证** |
| 决策闭环 | 配对、伤害、组队三页各自为战，没有"我手上这些怎么配最强"的主线 | ⚠️ 断裂 |

**核心结论**：产品的"读"和"查"已经很好了，坏在**数据不新**、**算法无护栏**、**新功能没被真正用过**。

---

## 3. 优先级重排：与外部审计报告的分歧

外部审计报告（P0/P1/P2）是**代码视角**，且基于今天 14:25 之前的旧快照——它列的三条 P0 已全部修复。

从产品视角，我给出不同的排序：

| 维度 | 报告的第一优先级 | 我的第一优先级 | 理由 |
|---|---|---|---|
| 关注点 | 代码结构（拆 4638 行文件、收敛 innerHTML） | 数据新鲜度 + 算法正确性 | 单人本地工具，"代码优雅"不产生用户价值；"数据过期"和"算错了"直接摧毁价值 |
| 风险观 | 潜在的 XSS、潜在的重复定义 | 实际的停更 8 天、实际的 0 测试 | 潜在风险 vs 已发生的事实 |

**这不代表报告错**——拆文件、收敛转义都是对的，只是排期应靠后。一台发动机漏油的赛车，先补油，再谈空气动力学。

---

## 4. 路线图

### P0 · 让产品可信（数据 + 备份 + 验证）

目标：确保任何时候打开这个工具，看到的都是**最新的、算对的、丢不了的**数据。

| # | 任务 | 具体动作 | 验收标准 |
|---|---|---|---|
| 0.1 | 数据滚到最新 | 跑 `python scripts/pipeline.py update`，检查变更报告 | `fetched_at` 变为今日；变更报告可读 |
| 0.2 | 数据更新常态化 | 接 WorkBuddy 自动化，每周固定时点跑 update + 备份 | 自动化任务存在且生效，无需手动记得 |
| 0.3 | 重建备份基线 | 备份瘦身后 24.6 MB 新库；删除 08-17 两份共 396 MB 旧备份 | `data/backup/` 保留 3 份，均为近期且体积正常 |
| 0.4 | 组队功能端到端验证 | 真的组一队、保存、刷新、重开浏览器、看是否还在 | `team` 表有数据；云端同步后也能拉回 |
| 0.5 | 清理残留 | `data/meta/_probe_gundam.db-shm`、`fetch.pid`、`data/gge_gundam.db` | 目录干净，无 0 字节/探针文件 |

### P1 · 让算法可信（测试护栏）

目标：改一行正则，能立刻知道有没有悄悄改变全站排名。

| # | 任务 | 具体动作 | 验收标准 |
|---|---|---|---|
| 1.1 | 建 `tests/` 骨架 | 引入 pytest，配最小 `pyproject.toml` | `pytest` 可运行 |
| 1.2 | 伤害公式金样例 | 对 `damage.calculate_damage` 用手算样例（来自 formulas.docx）固化 | 若干条断言覆盖公式主链 |
| 1.3 | 评分模型快照测试 | 对 `pairing` 典型机体+驾驶员组合，固化得分 | 分数变化会立刻暴露 |
| 1.4 | 解析函数金样例 | `labels.star_value` / `parse_weapon_max_level` / 11 组效果正则 | 522 条真实文案作为语料 |
| 1.5 | 敌方机体缺口处理 | 271 个敌方 `unit_id` 不在 `unit` 表（7 行 NULL） | UI 明确标注"敌方专用，仅关卡数值"或补抓 |

### P2 · 让决策闭环（产品价值）

目标：从"能查"进化到"告诉我该练谁"。

| # | 任务 | 具体动作 | 验收标准 |
|---|---|---|---|
| 2.1 | 组队页补全 | 基于 0.4 的验证结果，补齐缺失环节 | 能完整走完"选机体→选驾驶员→选支援→看评分→保存" |
| 2.2 | 三页联动 | 配对结果可一键加入队伍；队伍可一键进伤害计算 | 不再需要手动重试数据 |
| 2.3 | 密钥安全 | 轮换 Neon 密码，改系统环境变量注入 | `.env` 不再是唯一依赖 |
| 2.4 | 文档校正 | CODE_WIKI 表数 21→27、行数表、scripts 入口数 | 文档与实际一致 |

### P3 · 让产品耐用（可维护性）

目标：半年后还改得动。

| # | 任务 | 具体动作 |
|---|---|---|
| 3.1 | 后端路由表化 | `webapp.py` 34 条 `if path ==` 改 dict 分发，按域拆 units/characters/supporters/damage/team/cloud |
| 3.2 | 前端转义收敛 | 95 处 `innerHTML` 集中为 render 工具函数，或统一 textContent 约定 |
| 3.3 | 前端模块化 | `app.js` 5357 行按页拆分（不引入构建工具） |
| 3.4 | `_scratch` 归档说明 | 25 个一次性脚本加 README 标注不可运行 |

---

## 5. 明确不做的事

防止过度工程化——这是单人本地工具，不是 SaaS。

| 不做 | 理由 |
|---|---|
| ❌ 用户鉴权 / 多用户 | 单人使用，无攻击面 |
| ❌ 改成 `0.0.0.0` 监听 | 会立刻把 `/api/import`（512 MB 上传）和 `/api/unit-edit`（写库）变成开放口 |
| ❌ 引入前端构建链 / TypeScript | 5k 行自建工具，收益远小于维护成本 |
| ❌ 追求高测试覆盖率 | 只锁纯计算逻辑（伤害/评分/解析），不为 CRUD 写测试 |
| ❌ 为拆文件而拆文件 | P3 才做，且按域拆，不追求文件行数好看 |

---

## 6. 衡量标准

产品做得对不对，看这三个数：

1. **数据新鲜度**：`fetched_at` 距今 ≤ 7 天 —— 资料库的生命线
2. **算法可信度**：核心计算有测试，改动能立刻发现回归 —— 当前 0 → 目标覆盖 3 个模块
3. **功能真实使用率**：`team` 表非空 —— 做了的功能要真的被用过，否则等于没做

---

## 7. 阻塞：数据源失效（2026-09-10 发现）

跑 `pipeline.py update` 时报 `GET /ggetapi/zh-CN/series/v2` **HTTP 404**，返回的是站点 SPA 的 HTML 而非 JSON。

排查结论——**不是本项目的问题，也不是反爬**：

| 检查项 | 结果 |
|---|---|
| `ggetapi` / `limbusapi` / `socapi` 三个子站 API | 全部 404 |
| `https://soshage.com/gget/` 页面 | HTTP 500（HTML 能返回，title 正常） |
| 带浏览器 UA + Referer 重试 | 仍 404 |
| 页面 canonical | 变为 `https://soshage.com/gget/gget/`（双 gget，疑似改版） |
| 最后一次成功抓取 | 2026-09-02，8 天前正常 |

**判断**：站点整体故障或改版中。在这之前 `update` 与任何定时自动化都必然失败。

**应对**：
1. 等待站点恢复，或人工确认新 API 路径后更新 `src/config.py` 的 `API_BASE`
2. 现有数据（unit 1237 / character 593 / supporter 87 / stage 625）仍完整可用，产品功能不受影响
3. 数据源恢复后再启用 P0.2 的定时更新

---

## 附：当前基线快照（2026-09-10）

- 代码 18,754 行：`app.js` 5357 / `webapp.py` 4638 / `style.css` 2153 / `pairing.py` 1976 / `cloud.py` 1334 / `db.py` 1271
- 数据库 24.6 MB（09-10 由 198 MB 瘦身，提交 `7d2439e`），27 张表
- 数据 `fetched_at = 2026-09-02`，`built_at = 2026-09-02T09:58:18+0800`
- 测试：0 个；CI：无；lint：无
- 最近提交：`c7669a4`（防御配对死循环修复）
