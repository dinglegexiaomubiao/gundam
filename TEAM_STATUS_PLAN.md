# 规划：队伍状态摘要（队伍卡片底部信息行）

> 状态：**P0–P3 已实施（2026-09-21）**，P4（整队协同评分）待定。
> 实施偏差：原计划的 `src/team_insight.py` 改为直接实现在 `src/pairing.py`
> （`_weapon_damage` / `_range_info` / `_weapon_effects_classified` / `_unit_defense_insight` /
> `_pilot_synergy` / `_build_team_insights`）—— 这样能复用 pairing 已有的伤害口径、
> 条件解析与驾驶员缓存，避免两个模块重复实现同一套算法。
> 目标：让每支队伍的「怎么打、能不能打」一眼可读，而不是只看数值。

---

## 一、目标与定位

在每张队伍卡片**底部新增一块「队伍状态」**，把分散在机体/驾驶员/支援角色里的
**作战能力特征**浓缩成三段（攻击 / 支援 / 防御），并按队伍实际配置做**协同判定**。

不是重复展示数值，而是回答三个问题：

1. 这支队伍**怎么打**？（射程、伤害类型、最高伤害武器）
2. 支援**有没有生效**？（特效类型与攻击武器的类型是否匹配）
3. 能不能**扛住**？（移动力、减伤机制、驾驶员协同）

---

## 二、三段内容定义

### ① 攻击型（`unit.role = 1`）

| 展示项 | 口径 | 数据源 |
|---|---|---|
| 最大武器射程 | 该机**全部武器**中 `range_max` 的最大值，**排除 MAP 武器** | `unit_weapon.range_max` / `map_weapon_range` |
| 最高伤害武器 | 队伍里**为该槽位实际选中的武器**（与伤害计算口径一致）；未选武器则提示「未选武器」 | 前端 `slots[i].weapon` |
| 该武器伤害 | 复用 `/api/team/score` 已返回的 `pairs[].weapon.damage`，不重复计算 | ✅ 现成 |
| 该武器伤害类型 | 物理 / 光束 / 特殊（可多选） | `unit_weapon.weapon_attrs` ⚠️ 见第四节 |
| 依赖属性 | 射击 / 格斗 / 觉醒（已有 `dep_label`） | ✅ 现成 |

### ② 支援型（`unit.role = 3`）

| 展示项 | 口径 | 数据源 |
|---|---|---|
| 有哪些武器特效 | 扫描该机全部武器的 `weapon_effects`，按语义归类：<br>· 损伤提升类 → 物理 / 光束 / 特殊<br>· 通用类 → 防御力减少 等 | `unit_weapon.weapon_effects`（JSON） |
| 特效对应武器射程 | 每个命中特效取其来源武器的 `range_min-range_max` | ✅ 现成 |
| **匹配判定** | 支援提供的损伤提升类型集合 ∩ 攻击型最高伤害武器的伤害类型集合<br>命中数 / 需求数（如 `2/2`） | 见 P3 |

> 现有工程里已有同类先例可直接复用：
> `webapp.py` 的 `WFX_FILTERS` 用 `weapon_effects LIKE '%物理损伤提升%'` 等做筛选，
> 说明「按特效文本归类」这条路已经跑通。

### ③ 防御型（`unit.role = 2`）

| 展示项 | 口径 | 数据源 |
|---|---|---|
| 移动力 | `unit.movement`（升星后取 `max_movement`） | ✅ 现成 |
| 减伤机制 | 从 `unit_ability.traits` 抽取：武装属性减伤（`trait_type=79`）、<br>损伤阈值无效（`trait_type=14`）、防御力提升（`trait_type=9`）等 | ✅ 现成 |
| 单位技能 | `unit_skill.desc`（目前全库仅 4 行，均为 EX 机体） | ✅ 现成 |
| 驾驶员协同 | 驾驶员 `character_skill` / `character_ability.traits` 中可被本机体激活的项：<br>HP 归零恢复、MP 提升、移动力提升、EX 能力条件是否满足 | ✅ 现成（`traits.active_condition_set_id`） |

**已验证示例**（00强化模组 (最后决战式样) (EX)，`unit_id=1370005950`）：
- `unit_ability` → **GN力场 LV4** 的 `traits`：
  - `trait_type=79, value=20`：`发起战斗的敌方使用物理、光束武装攻击时，自身受到的损伤减轻20%`
  - `trait_type=14, value=4500`：`自身受到的损伤4500以下时，损伤无效`
- `unit_skill`：`自身攻击力提升25%（1回合）/ 自身防御力提升15%（1回合）（每场战斗1次）`
- `unit.movement = 5`

**已验证示例**（刹那·F·清英，`character_id=1370000100`）：
- 能力：`造成的损伤提升15%`(17) / `MP提升6`(46) / **EX角色能力**：`「额外行动」+1次`(19)
  + `搭乘单位含有上述「标签」时，自身受到的损伤减轻15%`(14, `cond=1000078`)
- 技能：`MP提升 LV4`(4) / `高速 LV1`：下次移动移动力+1(7) / `攻击爆裂 LV4`：造成损伤+20%(8)
- `support_info = {"extra":{"count":1,"cond":false}}`

> 你提到的「HP 为 0% 时恢复 7%（1 次）」不在 `1370000100` 上，
> 应是刹那的另一个形态（`1370003701` / `1370003702`）。**需确认你要看的是哪一个形态。**

---

## 三、⚠️ 前置阻塞：伤害类型映射不完整（必须先定口径）

「支援特效 ↔ 攻击武器类型」的匹配判定**完全依赖** `unit_weapon.weapon_attrs`（伤害类型集合），
但该字段目前**没有可靠的生成规则** —— 重新爬取会丢失已有数据。

### 证据：原始 `weapon_attr` × 数据库 `weapon_attrs` 交叉表

| 原始 `weapon_attr` | 数据库现值 | 武器数 | 说明 |
|---|---|---|---|
| 1 | `[1]` | 1705 | 实弹 ✅ 一致 |
| 2 | `[2]` | 2196 | 光束 ✅ 一致 |
| 3 | `[3]` | 639 | 特殊 ✅ 一致 |
| 4 | `[3]` | 17 | 特殊招式 → 特殊 ✅ |
| 4 | **`[2,1]`** | **1** | 光束+实弹（**手工修正过**） |
| 5 | **`[]`** | **35** | ← **被丢弃** |
| 5 | **`[1,3]`** | **2** | 实弹+特殊（**手工修正过**） |
| 6 | **`[]`** | **50** | ← **被丢弃** |
| 6 | **`[2,3]`** | **2** | 光束+特殊（**手工修正过**） |

### 问题所在

`src/db.py:_weapon_attrs_from_attr()` 的规则是
`1/2/3 → 单值`、`4 → [3]`、**`5/6 → []`**，即 4/5/6 里只有 4 有映射，5 和 6 一律清空。
而数据库里那 5 个「多类型」值是**手工改的**（`scripts/migrate_weapon_attack_attr.py`
的 `backfill_attrs()` 规则与 `db.py` 一致，不会产生多类型）。

后果：**只要重新点一次「爬取数据」重建数据库，这 5 把武器的伤害类型信息就会丢失**：

| 机体 | 武器 | 现值 | 重建后会变成 |
|---|---|---|---|
| 高达 (EX) | 光束军刀 EX | `[2, 1]` | `[3]` |
| 吉翁号 (EX) | 全领域攻击 EX | `[2, 3]` | `[]` |
| 夏亚专用渣古Ⅱ (THE ORIGIN版)(EX) | MS用对舰步枪 EX | `[1, 3]` | `[]` |
| 比格·朗格 (EX) | 大出力米加粒子炮 EX | `[2, 3]` | `[]` |
| 高达·巴巴托斯 天狼型帝王型态 | 解除阿赖耶识限制器 | `[1, 3]` | `[]` |

### 与你的描述对照

你说「神高达 (EX) 的最强武装**洗牌同盟拳 EX** 会得到**物理和特殊**两种损伤提升」。
实测 `洗牌同盟拳 EX` 的原始 `weapon_attr = 5`，而数据库现值是 **`[]`**（完全没有伤害类型）。

而另外两把同样 `weapon_attr = 5` 的武器（MS用对舰步枪 EX、解除阿赖耶识限制器）
数据库里是 **`[1, 3]` = 实弹（物理）+ 特殊** —— 完全对得上你的说法。

**高度怀疑正确映射是**（{1: 实弹/物理, 2: 光束, 3: 特殊}）：

| 原始值 | 推测含义 | 应为 |
|---|---|---|
| 4 | 光束 + 物理 | `[1, 2]` |
| 5 | 物理 + 特殊 | `[1, 3]` |
| 6 | 光束 + 特殊 | `[2, 3]` |

若成立，则 洗牌同盟拳 EX 应修正为 `[1, 3]`，且另有 **85 把** 4/5/6 类武器需要一并重算
（当前分别被写成 `[3]` 或 `[]`）。**这一条需要你按游戏实际规则拍板，我不能靠猜。**

---

## 四、分期实施计划

### P0 · 修数据口径（阻塞项，必须先做）

1. 确定 `weapon_attr` 4/5/6（以及 7 是否存在）的**正确伤害类型映射**；
2. 改 `src/db.py:_weapon_attrs_from_attr()`，让**重新爬取也能正确生成**（不再依赖手工修正）；
3. 同步 `scripts/migrate_weapon_attack_attr.py` 的 `backfill_attrs()` 规则，保持一致；
4. 写迁移脚本回填既有库（`--dry-run` + 自动备份，沿用 `migrate_character_sp.py` 的模式）；
5. 回归测试：断言 `洗牌同盟拳 EX → [1, 3]`，且**重建前后一致**（防止再次漂移）。

### P1 · 后端聚合（可单测，不碰前端）

新增 `src/team_insight.py`（或并入 `pairing.py`），产出每队的 `insights` 结构：

```
insights = {
  "attack":  {unit_id, name, max_range, max_range_nomap, weapon:{name, damage, attrs[]}},
  "support": {unit_id, name, effects:[{label, kind, range, source_weapon}]},
  "defense": {unit_id, name, movement, mitigations:[{label, value, attrs[]}],
              unit_skill, pilot:{...}},
  "match":   {attack_attrs:[], covered:[], missed:[], score:"2/2"}
}
```

- 全部为**只读查询**，走 `dbutil.connect_ro`；
- `max_range` 的「排除 MAP」判据沿用既有的
  `IFNULL(map_weapon_range,'') NOT IN ('','null','0')`（全库 224 把为 MAP）；
- 单测：`tests/test_team_insight.py`，用 神高达(EX) / 00强化模组(EX) / 刹那 作 fixture。

### P2 · 接口

两个方案（待定，见第五节）：
- **A（推荐）**：扩展 `/api/team/score` 的返回，增加 `insights`（少一次往返，与既有 `pairs` 同源）；
- **B**：新增 `POST /api/team/insights`，只算特征不算伤害（更快，但要多一次请求）。

### P3 · 前端渲染

- 在 `renderTeamRow()` 的队伍卡片末尾追加 `.team-status` 区块（三段 + 匹配徽标）；
- 视觉语言**复用现有类型令牌**：`--type-atk` 红 / `--type-tank` 蓝 / `--type-sup` 绿，
  左侧色条 + 小图标（三角/方块/圆形），与机体卡片保持一致；
- 匹配成功用绿胶囊（`2/2`），未命中用琥珀色胶囊（`0/2`）—— 与现有
  `.team-conflict` 横幅的告警色系一致；
- 回归：扩展 `tests/js/team_render_smoke.js` 或新增 `team_status_smoke.js`
  （沿用已有的 DOM 桩 + `vm` 手法，无需浏览器）。

### P4 ·（可选）协同评分

在三段基础上给一个整队协同结论，例如：
- 🟢 完美协同（支援类型全覆盖 + 防御机制完整）
- 🟡 部分协同（支援只覆盖一半）
- 🔴 支援无效（0 覆盖，或攻击型未选武器）

---

## 五、待确认的决策点

| # | 问题 | 我的建议 |
|---|---|---|
| 1 | **`weapon_attr` 4/5/6 的正确伤害类型映射**（阻塞项） | 4→光束+物理、5→物理+特殊、6→光束+特殊（需你确认） |
| 2 | 「最高伤害武器」用**实际选中**的武器，还是**理论最高伤害**的武器？ | 用实际选中的（与伤害计算一致，避免两套口径） |
| 3 | 三段按 `unit.role` 分派；某队若没有对应 role，该段**不显示**还是**显示「无」**？ | 不显示（节省空间），队伍顶部仍可加一行小结 |
| 4 | 匹配判定用**集合相交**（命中任一即算通过），还是要求**全覆盖**？ | 展示「命中数/需求数」（如 2/2、1/2），两种信息都在 |
| 5 | 看哪位驾驶员形态？你的例子提到「HP 归零恢复 7%」，但 `刹那·F·清英`(1370000100) 没有这条，应是另一形态 | 需你指定 `character_id`，或改为「遍历该角色的全部形态并合并展示」 |
| 6 | 接口方案 A（扩展 score）还是 B（新接口）？ | A：少一次往返，且与 `pairs` 同源不会不一致 |
| 7 | 是否要 P4 的整队协同评分？ | 建议先做 P0–P3，看实际观感再决定 |

---

## 六、影响文件（预估）

| 文件 | 改动 |
|---|---|
| `src/db.py` | `_weapon_attrs_from_attr()` 补全 4/5/6/7 映射 |
| `scripts/migrate_weapon_attack_attr.py` | 同步映射规则 |
| `scripts/migrate_weapon_attrs_v2.py`（新） | 回填既有库 |
| `src/team_insight.py`（新） | 队伍特征聚合 |
| `src/pairing.py` / `src/webapp.py` | 暴露 `insights` |
| `web/app.js` / `web/style.css` | 卡片底部区块 + 类型视觉 |
| `tests/test_team_insight.py`（新） | 后端单测 |
| `tests/js/team_status_smoke.js`（新） | 前端渲染冒烟 |
